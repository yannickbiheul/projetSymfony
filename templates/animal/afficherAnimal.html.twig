{% extends 'base.html.twig' %}

{% block title %}Page d'accueil{% endblock %}

{% block monTitre %} <h1>Animal choisi</h1> {% endblock %}

{% block body %}
    <h2>{{ animal.nom }}</h2>
    <p>Famille : {{ animal.famille }}</p>
    <p>Couleur : {{ animal.color }}</p>
    <p>Poids : {{ animal.poids }}</p>
    <br>
    <a href="{{ path('animal') }}">Retour</a>
{% endblock %}